<%= form_for [:front, @conversation, @message] do |f| %>
  <%= render "/shared/form_errors", :object => f.object %>
  <legend><%= "message attributes" %></legend>
  <hr>

  <div class="row mb-3">
    <%= f.label :role, class: "col-sm-2 col-form-label text-end"%>
    <div class="col-sm-10">
      <%= f.select :role, Message.roles, {}, class: "form-control" %>
    </div>
  </div>

  <div class="row mb-3">
    <%= f.label :body, class: "col-sm-2 col-form-label text-end"%>
    <div class="col-sm-10">
      <%= f.text_area :body, class: "form-control", placeholder: t("activerecord.attributes.message.body") %>
      <div class="form-text">Ask anything.</div>
    </div>
  </div>

  <div class="row">
    <div class="offset-sm-2 col">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
